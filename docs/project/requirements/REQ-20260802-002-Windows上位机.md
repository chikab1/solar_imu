# REQ-20260802-002 Windows上位机软件

状态：In Progress  
优先级：P0  
提出日期：2026-08-02  
目标版本：PC Tool V1.0

## 1. 原始需求

根据当前USART2二进制协议制作可在普通电脑使用的EXE上位机。选择COM口并连接后，软件自动发送00唤醒设备；用户可勾选Pitch和Roll持续查看数据。界面参考用户提供的传感器上位机截图，采用多个页面，每页负责不同功能。USART2串口通信指令手册中能够实现的功能均应提供图形化操作。

## 2. 当前事实

- 当前设备维护口为USART2，115200、8-N-1、无流控。
- Stop1通信必须执行单字节00、等待FF READY、再发送完整命令帧。
- 当前协议支持GET_STATUS、RUN_REPORT、SET_CONFIG、READ_QUEUE、CLEAR_QUEUE、SLEEP、MODEM_ON、MODEM_OFF、GET_IMU_DIAG、SET_MOUNT、GET_PITCH、GET_ROLL和GET_ANGLE。
- 当前协议不提供原始三轴加速度、三轴角速度或Yaw数据。
- 当前PC环境为Windows x64，已安装Python 3.13和pyserial；尚未安装PySide6和PyInstaller。
- 要做到目标电脑无Python环境也能运行，需要将运行时、GUI库和串口库一起打包。

## 3. 推荐技术路线

采用 Python + PySide6 + pyserial + PyInstaller：

- PySide6适合制作接近参考图片的现代多页面桌面界面。
- 串口收发放在独立工作线程，GUI线程不等待串口，避免窗口卡死。
- 所有协议帧由统一的ProtocolCodec生成和校验，不在按钮事件里硬编码CRC。
- 使用单请求队列和Sequence匹配，严格执行一问一答。
- 实时角度优先使用GET_ANGLE，一次读取Pitch和Roll，减少串口流量。
- 发布为Windows x64免安装版本，目标电脑不需要Python。
- 开发版本先使用onedir目录便于诊断，验收后再生成onefile单EXE。

预计单文件EXE体积约80~150 MB，首次启动可能需要数秒解压。最终大小以实际打包结果为准。

## 4. 建议页面

### 4.1 连接与总览

- 自动扫描和刷新COM口。
- 显示端口描述、USB VID/PID和当前连接状态。
- 点击连接后自动执行00 → READY。
- 自动重试唤醒并显示明确失败原因。
- 展示电池电压、4G状态、低压熔断、事件数量、IMU状态和最近上报结果。

### 4.2 实时角度

- Pitch、Roll独立勾选。
- 勾选任意一项后周期发送GET_ANGLE。
- 默认100 ms，可设置50~2000 ms。
- 大字体实时数值、角度仪表和滚动趋势曲线。
- 连接中断时停止轮询，不继续堆积请求。
- 可选CSV记录：时间、Pitch、Roll、连接状态。
- 可选二维或三维姿态示意；只有Pitch/Roll，没有真实Yaw。

### 4.3 参数设置

- Wake-Up阈值、倾角阈值、RTC周期、低压阈值。
- 安装零度方向Z+/Z-/X+/X-/Y+/Y-。
- 输入范围验证、发送前确认、成功/失败提示。
- 设置后自动GET_IMU_DIAG进行读回核对。

### 4.4 状态与诊断

- 完整解析GET_STATUS的63字节数据。
- 复位原因、上报阶段、失败原因转换为中文。
- 显示串口丢帧、半帧超时、UART错误和RTC/IMU计数。
- GET_IMU_DIAG关键寄存器支持十六进制与解释视图。

### 4.5 事件队列

- 读取0~2号事件。
- 将52字节EventRecord解析为表格。
- 支持刷新、CSV/JSON导出。
- CLEAR_QUEUE需要二次确认并提示低电压限制。

### 4.6 4G与上报

- ML307C开机、关机。
- 人工RUN_REPORT。
- 显示任务已接受、BUSY和失败状态。
- 上报期间周期查询状态需要避开BUSY阶段，不能阻塞串口请求队列。

### 4.7 串口调试与日志

- 时间戳、方向、功能码、Sequence、状态和原始Hex日志。
- 支持发送合法的自定义协议帧。
- CRC自动生成、响应CRC校验。
- 日志筛选、清空和导出。
- 默认隐藏危险操作，避免误清队列或误休眠。

### 4.8 关于与协议

- 软件版本、协议版本、固件兼容范围。
- 内置快速测试说明和状态码表。
- 链接到USART2串口通信指令手册。

## 5. 软件架构

建议目录：

~~~text
pc_tool/
├─ app/
│  ├─ main.py
│  ├─ protocol/
│  │  ├─ codec.py
│  │  ├─ commands.py
│  │  └─ models.py
│  ├─ serial/
│  │  ├─ worker.py
│  │  └─ session.py
│  ├─ services/
│  │  ├─ device_service.py
│  │  └─ export_service.py
│  ├─ ui/
│  │  ├─ main_window.py
│  │  ├─ theme.qss
│  │  └─ pages/
│  └─ resources/
├─ tests/
├─ requirements.txt
├─ build_exe.ps1
└─ README.md
~~~

关键状态机：

~~~text
Disconnected
  → PortOpen
  → SendWake00
  → WaitReady
  → Ready
  → SendOneRequest
  → WaitMatchingResponse
  → Ready
~~~

断开、超时、CRC错误或拔线会进入Recovering；实时轮询只能存在一条在途请求，防止串口阻塞和响应错配。

## 6. 第一期范围

包含：

- 当前USART2手册的全部命令。
- 多页面GUI。
- COM连接和自动唤醒。
- Pitch/Roll持续采集、趋势曲线和CSV。
- 状态、配置、事件、4G、IMU诊断和原始日志。
- Windows x64无Python依赖发布包。
- 协议单元测试、假串口测试和EXE启动测试。

暂不包含：

- 原始加速度、陀螺仪和Yaw实时显示，除非同步扩展固件协议。
- MQTT服务器直接登录和云端设备管理。
- 固件升级/串口Bootloader。
- Windows数字签名证书。
- macOS和Linux安装包。

## 7. 已确认结论

1. 仅支持Windows 10/11 x64，不考虑Windows 7和32位系统。
2. 最终交付采用免安装单文件EXE；开发和诊断阶段同时保留便携目录版。
3. V1包含Pitch/Roll数值、滚动趋势曲线和简化3D板卡姿态。
4. 实时角度默认100 ms轮询，允许用户调整。
5. V1严格使用现有USART2协议，不修改STM32固件。
6. 六轴页面保留界面和协议扩展提示，等后续增加GET_IMU_RAW后启用。
7. 软件暂定名称为“Solar IMU 工业监测上位机”，后续可只改品牌资源而不改变架构。

## 8. 验收标准

- [ ] Windows 10/11 x64无Python环境可直接运行发布包。
- [x] 自动列出COM口，连接后完成00 → READY（模拟设备自动化验证通过）。
- [ ] 拔插串口、错误COM、无设备和超时不会导致GUI卡死。
- [x] 所有当前USART2命令均有可操作页面。
- [ ] Pitch/Roll连续运行30分钟无请求堆积、界面卡顿或响应错配。
- [x] CRC、半包、粘包和噪声恢复已有自动化测试；Sequence严格匹配，BUSY已实现1秒退避。
- [x] 配置值有范围校验，清空事件队列有二次确认。
- [x] CSV/JSON导出字段与协议一致。
- [ ] EXE在至少两台未安装开发环境的Windows电脑验证。
- [ ] 使用说明和打包说明完整。

## 9. 风险

- PySide6单文件体积较大，杀毒软件可能对未签名的新EXE产生信誉提示。
- 单文件模式启动比目录模式慢，调试阶段优先使用目录模式。
- 100 ms轮询会让设备持续处于维护窗口，不会进入Stop1，这是用户主动实时监视时的预期行为。
- 上报期间固件返回BUSY，上位机必须退避而不是高频重试。
- 现有协议没有设备固件版本查询，软件暂时只能按协议V1判断兼容性。

## 10. 2026-08-02 实现记录

- 已完成 `pc_tool/app` 协议层、模拟设备、独立串口线程、八个功能页面和现代化样式。
- 已完成 8 项自动化测试，结果为 `8 passed in 1.60s`。
- 已生成并启动验证目录版和免安装单文件版。
- 单文件路径：`pc_tool/dist/SolarIMU_Tool.exe`，大小 61,883,263 字节。
- 单文件 SHA-256：`05B927EC133C088508E207FF8333C39A1F0D93E6EDDA529A7D25ABB2D3DA7A85`。
- 当前仍保持 In Progress：需要用户用真实 CH340 + STM32 完成实机协议、拔插和长时间采集验收，并在另一台无开发环境的 Win10/11 x64 电脑验证。

## 11. 安装包与QtCore问题修复

- 用户报告原发布包出现 `ImportError: DLL load failed while importing QtCore`。
- 已复现为 WinError 127。根因是构建机PATH中的Anaconda `icuuc.dll/icudt73.dll` 被PyInstaller错误收集，覆盖Windows系统ICU接口。
- 构建脚本现在会将DLL搜索路径隔离为Windows系统目录，避免Anaconda、Matlab和SDK运行库污染发布包。
- 已生成标准Inno Setup安装程序，默认安装到当前用户目录并创建桌面快捷方式，无需管理员权限、Python或手工管理DLL。
- 最终安装包已通过：安装返回0、安装后的程序启动返回0、卸载返回0、临时安装目录无残留。
- 安装包：`pc_tool/dist/SolarIMU_Tool_Setup_v1.0.0_Win64.exe`，大小31,744,481字节。
- 安装包SHA-256：`C282AC805BAD62094750B0EAF5A09AAD397D34A60BA7452A74CE61C7D2EC2A68`。
- 修复后的单文件版也通过独立进程启动验证，SHA-256：`5D463C3AA851D8728C6998332F2E02B8B1A6ADBA373DA9BD66F5F87638DF4F70`。

## 12. V1.0.1 CH340连接修复

- 打开串口前预置DTR/RTS为低，避免pyserial默认拉高控制线与XCOM行为不同。
- 串口打开后等待300 ms让CH340和Windows驱动稳定，所有写操作增加flush。
- 唤醒从固定3次、每次2秒改为6次自适应间隔，更适合Stop1首字节牺牲和GPIO转UART的时序。
- 同时识别二进制READY、`[SYS] UART WAKE READY`和`[SYS] Ready`文本兼容响应。
- READY丢失时自动发送Sequence为FE的GET_STATUS探测；收到合法响应也可确认链路就绪。
- 失败提示包含最后64字节RX原始Hex，不再被通用“串口已断开”覆盖。
- 修复连接失败后工作线程已销毁但客户端仍持有引用导致的再次连接异常。
- 新增3项唤醒兼容性测试，总计11项测试通过。
- 当前无法实测CH340：构建时Windows只检测到COM3/COM4蓝牙串口，没有检测到截图中的COM7。
- V1.0.1安装包：`pc_tool/dist/SolarIMU_Tool_Setup_v1.0.1_Win64.exe`，31,743,525字节。
- SHA-256：`64752351CACD258606B82B7D1CBBCAD8AA3E8467429B16EA387426A42F2AB82E`。
